# QRCode Generator WebApp using Python(Flask)

This is a simple Web App that takes some text as input and generate a QRCode. There is also a download button from which the user can download the QR Code